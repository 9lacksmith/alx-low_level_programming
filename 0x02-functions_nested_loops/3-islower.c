#include "main.h"

/**
 * _islower - checks for the lowercase character
 * @c: the charcter to check
 *	Return: 1 if c is lower case
 *	Return : 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
